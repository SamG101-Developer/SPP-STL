use std::alloc::Alloc
use std::alloc::GlobalAlloc
use std::shared::Shared


cls Shadow[T, A: Alloc[T] = GlobalAlloc[T]] { }


sup [T] Shadow[T] {
    type Element = T

    @public
    @compiler_builtin
    fun upgrade(&self) -> Shared[T] { }
}
