use std::option::Opt
use std::option::Some


cls From[That] { }


sup [That] From[That] {
    @public
    @abstract_method
    fun from(that: That) -> Self { }

    @public
    @virtual_method
    fun try_from(that: That) -> Opt[Self] {
        ret Some(Self::from(that))
    }
}
