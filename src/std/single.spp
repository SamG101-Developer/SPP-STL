cls Single[T] { }

sup [T] Single[T] {
    @public
    @compiler_builtin
    fun new() -> Single[T] { }
}

sup [T] Single[T] ext ops::DerefRef[T] {
    @public
    @compiler_builtin
    cor deref_ref(&self) -> Gen[&T] { }
}

sup [T] Single[T] ext ops::DerefMut[T] {
    @public
    @compiler_builtin
    cor deref_mut(&mut self) -> Gen[&mut T] { }
}
