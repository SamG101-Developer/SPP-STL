use std::ops::deref::{DerefRef, DerefMut}
use std::generator::Gen


cls Single[T] { }


sup [T] Single[T] ext DerefRef[T] {
    @public
    @compiler_builtin
    cor deref_ref(&self) -> GenOnce[&T] { }
}


sup [T] Single[T] ext DerefMut[T] {
    @public
    @compiler_builtin
    cor deref_mut(&mut self) -> GenOnce[&mut T] { }
}
