use std::boolean::Bool


cls Try[Output, Residual] {}


sup [Output, Residual] Try[Output, Residual] {
    @private
    @inline
    @abstract_method
    fun op_is_residual(&self) -> Bool { }

    @private
    @inline
    @abstract_method
    fun op_as_residual(self) -> Residual { }
}
