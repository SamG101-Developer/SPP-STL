use std::boolean::Bool


@public cls Try[Output, Residual] {}


sup [Output, Residual] Try[Output, Residual] {
    @protected
    @inline
    @abstract_method
    fun op_is_output(&self) -> Bool { }

    @protected
    @inline
    @abstract_method
    fun op_as_output(self) -> Output { }
}
