use std::annotations::abstract_method
use std::annotations::public
use std::boolean::Bool

!public cls Ior[Rhs=Self] { }

# Allow the use of the "or" operator.
sup [Rhs] Ior[Rhs] {
    !public
    !abstract_method
    fun ior_(self, that: Rhs) -> Bool { }
}
