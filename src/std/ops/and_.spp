use std::annotations::abstract_method
use std::annotations::public
use std::boolean::Bool

!public cls And[Rhs=Self] { }

# Allow the use of the "and" operator.
sup [Rhs] And[Rhs] {
    !public
    !abstract_method
    fun and_(self, that: Rhs) -> Bool { }
}
