use std::boolean::Bool


!public cls Eq[Rhs] { }


sup [Rhs] Eq[Rhs] {
    !public
    !abstract_method
    fun eq(&self, that: &Rhs) -> Bool { }

    !public
    !virtual_method
    fun ne(&self, that: &Rhs) -> Bool {
        ret self.eq(that).not
    }
}
