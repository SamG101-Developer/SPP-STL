use std::boolean::Bool
use std::option::None
use std::void::Void


@public
cls Gen[Yield, Err=Void, Send=Void] { }
sup [Yield, Send] Gen[Yield, Send] {
    @private
    @compiler_builtin
    fun resume(&mut self, value: Send) -> Void { }
}


@public
cls GenOnce[Yield, Send=Void] { }
sup [Yield, Send] GenOnce[Yield, Send] {
    @private
    @compiler_builtin
    fun resume(&mut self, value: Send) -> Void { }
}


@private
cls Generated[Yield] { }
