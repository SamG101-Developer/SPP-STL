use std::ffi::CChar
use std::ffi::CInt


@ffi
fun fputs(str: &CChar, stream: &CInt) -> CInt { }

@ffi
fun fgets(buffer: &mut CChar, count: CInt, stream: &CInt) -> CChar { }

@ffi
fun fopen(filename: &CChar, mode: &CChar) -> CInt { }

@ffi
fun fread(buffer: &mut CChar, size: CInt, count: CInt, stream: &mut CInt) -> CInt { }

@ffi
fun fwrite(buffer: &mut CChar, size: CInt, count: CInt, stream: &mut CInt) -> CInt { }

@ffi
fun fclose(stream: &mut CInt) -> CInt { }
